This application lets you configure which applications are automatically
run as Administrator by Windows Explorer.

You can select the applications by dragging a target over a application
window or you can drop an application binary into the application.

This application modifies the following Registry Key:

	Software\Microsoft\Windows NT\CurrentVersion\AppCompatFlags\Layers
